# Outputs

`reported_results/` contains structured CSV transcriptions of numerical results reported in the submitted final report. These files are report-derived; they are not newly recomputed outputs.
